shadow$provide.module$node_modules$$codemirror$commands$dist$index_cjs = function(global, require, module, exports) {
  function command(f, option) {
    return ({state, dispatch}) => {
      if (state.readOnly) {
        return !1;
      }
      let tr = f(option, state);
      if (!tr) {
        return !1;
      }
      dispatch(state.update(tr));
      return !0;
    };
  }
  function getConfig(state, pos) {
    state = state.languageDataAt("commentTokens", pos, 1);
    return state.length ? state[0] : {};
  }
  function findBlockComment(state, {open, close}, from, to) {
    var textBefore = state.sliceDoc(from - 50, from), textAfter = state.sliceDoc(to, to + 50), spaceBefore = /\s*$/.exec(textBefore)[0].length, spaceAfter = /^\s*/.exec(textAfter)[0].length;
    let beforeOff = textBefore.length - spaceBefore;
    if (textBefore.slice(beforeOff - open.length, beforeOff) == open && textAfter.slice(spaceAfter, spaceAfter + close.length) == close) {
      return {open:{pos:from - spaceBefore, margin:spaceBefore && 1}, close:{pos:to + spaceAfter, margin:spaceAfter && 1}};
    }
    100 >= to - from ? textBefore = state = state.sliceDoc(from, to) : (textBefore = state.sliceDoc(from, from + 50), state = state.sliceDoc(to - 50, to));
    textAfter = /^\s*/.exec(textBefore)[0].length;
    spaceBefore = /\s*$/.exec(state)[0].length;
    spaceAfter = state.length - spaceBefore - close.length;
    return textBefore.slice(textAfter, textAfter + open.length) == open && state.slice(spaceAfter, spaceAfter + close.length) == close ? {open:{pos:from + textAfter + open.length, margin:/\s/.test(textBefore.charAt(textAfter + open.length)) ? 1 : 0}, close:{pos:to - spaceBefore - close.length, margin:/\s/.test(state.charAt(spaceAfter - 1)) ? 1 : 0}} : null;
  }
  function changeBlockComment(option, state, ranges = state.selection.ranges) {
    let tokens = ranges.map(r => getConfig(state, r.from).block);
    if (!tokens.every(c => c)) {
      return null;
    }
    let comments = ranges.map((r, i) => findBlockComment(state, tokens[i], r.from, r.to));
    if (2 != option && !comments.every(c => c)) {
      return {changes:state.changes(ranges.map((range, i) => comments[i] ? [] : [{from:range.from, insert:tokens[i].open + " "}, {from:range.to, insert:" " + tokens[i].close}]))};
    }
    if (1 != option && comments.some(c => c)) {
      option = [];
      for (let i = 0, comment; i < comments.length; i++) {
        if (comment = comments[i]) {
          ranges = tokens[i];
          let {open, close} = comment;
          option.push({from:open.pos - ranges.open.length, to:open.pos + open.margin}, {from:close.pos - close.margin, to:close.pos + ranges.close.length});
        }
      }
      return {changes:option};
    }
    return null;
  }
  function changeLineComment(option, state, ranges = state.selection.ranges) {
    var lines = [];
    let prevLine = -1;
    a: for (let {from, to} of ranges) {
      ranges = lines.length;
      let minIndent = 1e9, token;
      for (var pos = from; pos <= to;) {
        pos = state.doc.lineAt(pos);
        if (void 0 == token && (token = getConfig(state, pos.from).line, !token)) {
          continue a;
        }
        if (pos.from > prevLine && (from == to || to > pos.from)) {
          prevLine = pos.from;
          let indent = /^\s*/.exec(pos.text)[0].length, empty = indent == pos.length, comment = pos.text.slice(indent, indent + token.length) == token ? indent : -1;
          indent < pos.text.length && indent < minIndent && (minIndent = indent);
          lines.push({line:pos, comment, token, indent, empty, single:!1});
        }
        pos = pos.to + 1;
      }
      if (1e9 > minIndent) {
        for (pos = ranges; pos < lines.length; pos++) {
          lines[pos].indent < lines[pos].line.text.length && (lines[pos].indent = minIndent);
        }
      }
      lines.length == ranges + 1 && (lines[ranges].single = !0);
    }
    if (2 != option && lines.some(l => 0 > l.comment && (!l.empty || l.single))) {
      option = [];
      for (let {line, token, indent, empty, single} of lines) {
        !single && empty || option.push({from:line.from + indent, insert:token + " "});
      }
      lines = state.changes(option);
      return {changes:lines, selection:state.selection.map(lines, 1)};
    }
    if (1 != option && lines.some(l => 0 <= l.comment)) {
      state = [];
      for (let {line, comment, token} of lines) {
        0 <= comment && (lines = line.from + comment, option = lines + token.length, " " == line.text[option - line.from] && option++, state.push({from:lines, to:option}));
      }
      return {changes:state};
    }
    return null;
  }
  function cmd(side, selection) {
    return function({state, dispatch}) {
      if (!selection && state.readOnly) {
        return !1;
      }
      let historyState = state.field(historyField_, !1);
      if (!historyState) {
        return !1;
      }
      state = historyState.pop(side, state, selection);
      if (!state) {
        return !1;
      }
      dispatch(state);
      return !0;
    };
  }
  function depth(side) {
    return function(state) {
      state = state.field(historyField_, !1);
      if (!state) {
        return 0;
      }
      state = 0 == side ? state.done : state.undone;
      return state.length - (state.length && !state[0].changes ? 1 : 0);
    };
  }
  function updateBranch(branch, to, maxLen, newEvent) {
    branch = branch.slice(to + 1 > maxLen + 20 ? to - maxLen - 1 : 0, to);
    branch.push(newEvent);
    return branch;
  }
  function isAdjacent(a, b) {
    let ranges = [], isAdjacent = !1;
    a.iterChangedRanges((f, t) => ranges.push(f, t));
    b.iterChangedRanges((_f, _t, f, t) => {
      for (_f = 0; _f < ranges.length;) {
        _t = ranges[_f++];
        let to = ranges[_f++];
        t >= _t && f <= to && (isAdjacent = !0);
      }
    });
    return isAdjacent;
  }
  function eqSelectionShape(a, b) {
    return a.ranges.length == b.ranges.length && 0 === a.ranges.filter((r, i) => r.empty != b.ranges[i].empty).length;
  }
  function conc(a, b) {
    return a.length ? b.length ? a.concat(b) : a : b;
  }
  function addSelection(branch, selection) {
    if (branch.length) {
      let lastEvent = branch[branch.length - 1], sels = lastEvent.selectionsAfter.slice(Math.max(0, lastEvent.selectionsAfter.length - 200));
      if (sels.length && sels[sels.length - 1].eq(selection)) {
        return branch;
      }
      sels.push(selection);
      return updateBranch(branch, branch.length - 1, 1e9, lastEvent.setSelAfter(sels));
    }
    return [HistEvent.selection([selection])];
  }
  function addMappingToBranch(branch, mapping) {
    if (!branch.length) {
      return branch;
    }
    let length = branch.length;
    for (var selections = none; length;) {
      selections = mapEvent(branch[length - 1], mapping, selections);
      if (selections.changes && !selections.changes.empty || selections.effects.length) {
        return branch = branch.slice(0, length), branch[length - 1] = selections, branch;
      }
      mapping = selections.mapped;
      length--;
      selections = selections.selectionsAfter;
    }
    return selections.length ? [HistEvent.selection(selections)] : none;
  }
  function mapEvent(event, mapping, extraSelections) {
    extraSelections = conc(event.selectionsAfter.length ? event.selectionsAfter.map(s => s.map(mapping)) : none, extraSelections);
    if (!event.changes) {
      return HistEvent.selection(extraSelections);
    }
    let mappedChanges = event.changes.map(mapping), before = mapping.mapDesc(event.changes, !0), fullMapping = event.mapped ? event.mapped.composeDesc(before) : before;
    return new HistEvent(mappedChanges, state.StateEffect.mapEffects(event.effects, mapping), fullMapping, event.startSelection.map(before), extraSelections);
  }
  function updateSel(sel, by) {
    return state.EditorSelection.create(sel.ranges.map(by), sel.mainIndex);
  }
  function setSel(state, selection) {
    return state.update({selection, scrollIntoView:!0, userEvent:"select"});
  }
  function moveSel({state, dispatch}, how) {
    how = updateSel(state.selection, how);
    if (how.eq(state.selection, !0)) {
      return !1;
    }
    dispatch(setSel(state, how));
    return !0;
  }
  function rangeEnd(range, forward) {
    return state.EditorSelection.cursor(forward ? range.to : range.from);
  }
  function cursorByChar(view, forward) {
    return moveSel(view, range => range.empty ? view.moveByChar(range, forward) : rangeEnd(range, forward));
  }
  function ltrAtCursor(view$1) {
    return view$1.textDirectionAt(view$1.state.selection.main.head) == view.Direction.LTR;
  }
  function byCharLogical(state$1, range, forward) {
    range = range.head;
    let line = state$1.doc.lineAt(range);
    range = range == (forward ? line.to : line.from) ? forward ? Math.min(state$1.doc.length, line.to + 1) : Math.max(0, line.from - 1) : line.from + state.findClusterBreak(line.text, range - line.from, forward);
    return state.EditorSelection.cursor(range, forward ? -1 : 1);
  }
  function moveByCharLogical(target, forward) {
    return moveSel(target, range => range.empty ? byCharLogical(target.state, range, forward) : rangeEnd(range, forward));
  }
  function cursorByGroup(view, forward) {
    return moveSel(view, range => range.empty ? view.moveByGroup(range, forward) : rangeEnd(range, forward));
  }
  function toGroupStart(view, pos, start) {
    let categorize = view.state.charCategorizer(pos), cat = categorize(start), initial = cat != state.CharCategory.Space;
    return next => {
      next = categorize(next);
      if (next != state.CharCategory.Space) {
        return initial && next == cat;
      }
      initial = !1;
      return !0;
    };
  }
  function moveBySubword(view, range, forward) {
    let categorize = view.state.charCategorizer(range.from), cat = state.CharCategory.Space, pos = range.from, steps = 0, done = !1, sawUpper = !1, sawLower = !1, step = next => {
      if (done) {
        return !1;
      }
      pos += forward ? next.length : -next.length;
      let nextCat = categorize(next), ahead;
      nextCat == state.CharCategory.Word && 128 > next.charCodeAt(0) && /[\W_]/.test(next) && (nextCat = -1);
      cat == state.CharCategory.Space && (cat = nextCat);
      if (cat != nextCat) {
        return !1;
      }
      if (cat == state.CharCategory.Word) {
        if (next.toLowerCase() == next) {
          if (!forward && sawUpper) {
            return !1;
          }
          sawLower = !0;
        } else if (sawLower) {
          if (forward) {
            return !1;
          }
          done = !0;
        } else {
          if (sawUpper && forward && categorize(ahead = view.state.sliceDoc(pos, pos + 1)) == state.CharCategory.Word && ahead.toLowerCase() == ahead) {
            return !1;
          }
          sawUpper = !0;
        }
      }
      steps++;
      return !0;
    }, end = view.moveByChar(range, forward, start => {
      step(start);
      return step;
    });
    if (segmenter && cat == state.CharCategory.Word && end.from == range.from + steps * (forward ? 1 : -1)) {
      var skipped = view.state.sliceDoc(Math.min(range.head, end.head), Math.max(range.head, end.head));
      if (1 < skipped.length && /[\u4E00-\uffff]/.test(skipped) && (skipped = Array.from(segmenter.segment(skipped)), 1 < skipped.length)) {
        return forward ? state.EditorSelection.cursor(range.head + skipped[1].index, -1) : state.EditorSelection.cursor(end.head + skipped[skipped.length - 1].index, 1);
      }
    }
    return end;
  }
  function cursorBySubword(view, forward) {
    return moveSel(view, range => range.empty ? moveBySubword(view, range, forward) : rangeEnd(range, forward));
  }
  function moveBySyntax(state$1, start, forward) {
    let pos = language.syntaxTree(state$1).resolveInner(start.head), bracketProp = forward ? common.NodeProp.closedBy : common.NodeProp.openedBy;
    for (start = start.head;;) {
      let next = forward ? pos.childAfter(start) : pos.childBefore(start);
      if (!next) {
        break;
      }
      a: {
        var JSCompiler_inline_result = state$1;
        var node = next;
        if (node.type.prop(bracketProp)) {
          JSCompiler_inline_result = !0;
          break a;
        }
        let len = node.to - node.from;
        JSCompiler_inline_result = len && (2 < len || /[^\s,.;:]/.test(JSCompiler_inline_result.sliceDoc(node.from, node.to))) || node.firstChild;
      }
      JSCompiler_inline_result ? pos = next : start = forward ? next.to : next.from;
    }
    let match;
    state$1 = pos.type.prop(bracketProp) && (match = forward ? language.matchBrackets(state$1, pos.from, 1) : language.matchBrackets(state$1, pos.to, -1)) && match.matched ? forward ? match.end.to : match.end.from : forward ? pos.to : pos.from;
    return state.EditorSelection.cursor(state$1, forward ? -1 : 1);
  }
  function cursorByLine(view, forward) {
    return moveSel(view, range => {
      if (!range.empty) {
        return rangeEnd(range, forward);
      }
      let moved = view.moveVertically(range, forward);
      return moved.head != range.head ? moved : view.moveToLineBoundary(range, forward);
    });
  }
  function pageInfo(view$1) {
    let selfScroll = view$1.scrollDOM.clientHeight < view$1.scrollDOM.scrollHeight - 2, marginTop = 0, marginBottom = 0;
    if (selfScroll) {
      for (height of view$1.state.facet(view.EditorView.scrollMargins)) {
        let margins = height(view$1);
        if (null === margins || void 0 === margins ? 0 : margins.top) {
          marginTop = Math.max(null === margins || void 0 === margins ? void 0 : margins.top, marginTop);
        }
        if (null === margins || void 0 === margins ? 0 : margins.bottom) {
          marginBottom = Math.max(null === margins || void 0 === margins ? void 0 : margins.bottom, marginBottom);
        }
      }
      var height = view$1.scrollDOM.clientHeight - marginTop - marginBottom;
    } else {
      height = (view$1.dom.ownerDocument.defaultView || window).innerHeight;
    }
    return {marginTop, marginBottom, selfScroll, height:Math.max(view$1.defaultLineHeight, height - 5)};
  }
  function cursorByPage(view$1, forward) {
    let page = pageInfo(view$1), {state} = view$1, selection = updateSel(state.selection, range => range.empty ? view$1.moveVertically(range, forward, page.height) : rangeEnd(range, forward));
    if (selection.eq(state.selection)) {
      return !1;
    }
    let effect;
    if (page.selfScroll) {
      let startPos = view$1.coordsAtPos(state.selection.main.head);
      var scrollRect = view$1.scrollDOM.getBoundingClientRect();
      let scrollTop = scrollRect.top + page.marginTop;
      scrollRect = scrollRect.bottom - page.marginBottom;
      startPos && startPos.top > scrollTop && startPos.bottom < scrollRect && (effect = view.EditorView.scrollIntoView(selection.main.head, {y:"start", yMargin:startPos.top - scrollTop}));
    }
    view$1.dispatch(setSel(state, selection), {effects:effect});
    return !0;
  }
  function moveByLineBoundary(view, start, forward) {
    let line = view.lineBlockAt(start.head), moved = view.moveToLineBoundary(start, forward);
    moved.head == start.head && moved.head != (forward ? line.to : line.from) && (moved = view.moveToLineBoundary(start, forward, !1));
    !forward && moved.head == line.from && line.length && (view = /^\s*/.exec(view.state.sliceDoc(line.from, Math.min(line.from + 100, line.to)))[0].length) && start.head != line.from + view && (moved = state.EditorSelection.cursor(line.from + view));
    return moved;
  }
  function toMatchingBracket(state$1, dispatch, extend) {
    let found = !1, selection = updateSel(state$1.selection, range => {
      var matching = language.matchBrackets(state$1, range.head, -1) || language.matchBrackets(state$1, range.head, 1) || 0 < range.head && language.matchBrackets(state$1, range.head - 1, 1) || range.head < state$1.doc.length && language.matchBrackets(state$1, range.head + 1, -1);
      if (!matching || !matching.end) {
        return range;
      }
      found = !0;
      matching = matching.start.from == range.head ? matching.end.to : matching.end.from;
      return extend ? state.EditorSelection.range(range.anchor, matching) : state.EditorSelection.cursor(matching);
    });
    if (!found) {
      return !1;
    }
    dispatch(setSel(state$1, selection));
    return !0;
  }
  function extendSel(target, how) {
    let selection = updateSel(target.state.selection, range => {
      let head = how(range);
      return state.EditorSelection.range(range.anchor, head.head, head.goalColumn, head.bidiLevel || void 0);
    });
    if (selection.eq(target.state.selection)) {
      return !1;
    }
    target.dispatch(setSel(target.state, selection));
    return !0;
  }
  function selectByChar(view, forward) {
    return extendSel(view, range => view.moveByChar(range, forward));
  }
  function selectByGroup(view, forward) {
    return extendSel(view, range => view.moveByGroup(range, forward));
  }
  function selectBySubword(view, forward) {
    return extendSel(view, range => moveBySubword(view, range, forward));
  }
  function selectByLine(view, forward) {
    return extendSel(view, range => view.moveVertically(range, forward));
  }
  function selectByPage(view, forward) {
    return extendSel(view, range => view.moveVertically(range, forward, pageInfo(view).height));
  }
  function addCursorVertically(view, forward) {
    let {state:state$1} = view, sel = state$1.selection, ranges = state$1.selection.ranges.slice();
    for (let range of state$1.selection.ranges) {
      let line = state$1.doc.lineAt(range.head);
      if (forward ? line.to < view.state.doc.length : 0 < line.from) {
        for (let cur = range;;) {
          let next = view.moveVertically(cur, forward);
          if (next.head < line.from || next.head > line.to) {
            ranges.some(r => r.head == next.head) || ranges.push(next);
            break;
          } else if (next.head == cur.head) {
            break;
          } else {
            cur = next;
          }
        }
      }
    }
    if (ranges.length == sel.ranges.length) {
      return !1;
    }
    view.dispatch(setSel(state$1, state.EditorSelection.create(ranges, ranges.length - 1)));
    return !0;
  }
  function deleteBy(target, by) {
    if (target.state.readOnly) {
      return !1;
    }
    let event = "delete.selection", {state:state$1} = target, changes = state$1.changeByRange(range => {
      let {from, to} = range;
      if (from == to) {
        let towards = by(range);
        towards < from ? (event = "delete.backward", towards = skipAtomic(target, towards, !1)) : towards > from && (event = "delete.forward", towards = skipAtomic(target, towards, !0));
        from = Math.min(from, towards);
        to = Math.max(to, towards);
      } else {
        from = skipAtomic(target, from, !1), to = skipAtomic(target, to, !0);
      }
      return from == to ? {range} : {changes:{from, to}, range:state.EditorSelection.cursor(from, from < range.head ? -1 : 1)};
    });
    if (changes.changes.empty) {
      return !1;
    }
    target.dispatch(state$1.update(changes, {scrollIntoView:!0, userEvent:event, effects:"delete.selection" == event ? view.EditorView.announce.of(state$1.phrase("Selection deleted")) : void 0}));
    return !0;
  }
  function skipAtomic(target, pos, forward) {
    if (target instanceof view.EditorView) {
      for (let ranges of target.state.facet(view.EditorView.atomicRanges).map(f => f(target))) {
        ranges.between(pos, pos, (from, to) => {
          from < pos && to > pos && (pos = forward ? to : from);
        });
      }
    }
    return pos;
  }
  function selectedLineBlocks(state) {
    let blocks = [];
    var upto = -1;
    for (let range of state.selection.ranges) {
      let startLine = state.doc.lineAt(range.from), endLine = state.doc.lineAt(range.to);
      range.empty || range.to != endLine.from || (endLine = state.doc.lineAt(range.to - 1));
      upto >= startLine.number ? (upto = blocks[blocks.length - 1], upto.to = endLine.to, upto.ranges.push(range)) : blocks.push({from:startLine.from, to:endLine.to, ranges:[range]});
      upto = endLine.number + 1;
    }
    return blocks;
  }
  function moveLine(state$1, dispatch, forward) {
    if (state$1.readOnly) {
      return !1;
    }
    let changes = [], ranges = [];
    for (let block of selectedLineBlocks(state$1)) {
      if (forward ? block.to == state$1.doc.length : 0 == block.from) {
        continue;
      }
      let nextLine = state$1.doc.lineAt(forward ? block.to + 1 : block.from - 1), size = nextLine.length + 1;
      if (forward) {
        changes.push({from:block.to, to:nextLine.to}, {from:block.from, insert:nextLine.text + state$1.lineBreak});
        for (let r of block.ranges) {
          ranges.push(state.EditorSelection.range(Math.min(state$1.doc.length, r.anchor + size), Math.min(state$1.doc.length, r.head + size)));
        }
      } else {
        changes.push({from:nextLine.from, to:block.from}, {from:block.to, insert:state$1.lineBreak + nextLine.text});
        for (let r of block.ranges) {
          ranges.push(state.EditorSelection.range(r.anchor - size, r.head - size));
        }
      }
    }
    if (!changes.length) {
      return !1;
    }
    dispatch(state$1.update({changes, scrollIntoView:!0, selection:state.EditorSelection.create(ranges, state$1.selection.mainIndex), userEvent:"move.line"}));
    return !0;
  }
  function copyLine(state, dispatch, forward) {
    if (state.readOnly) {
      return !1;
    }
    var changes = [];
    for (let block of selectedLineBlocks(state)) {
      forward ? changes.push({from:block.from, insert:state.doc.slice(block.from, block.to) + state.lineBreak}) : changes.push({from:block.to, insert:state.lineBreak + state.doc.slice(block.from, block.to)});
    }
    changes = state.changes(changes);
    dispatch(state.update({changes, selection:state.selection.map(changes, forward ? 1 : -1), scrollIntoView:!0, userEvent:"input.copyline"}));
    return !0;
  }
  function newlineAndIndent(atEof) {
    return ({state:state$1, dispatch}) => {
      if (state$1.readOnly) {
        return !1;
      }
      let changes = state$1.changeByRange(range => {
        let {from, to} = range;
        range = state$1.doc.lineAt(from);
        var JSCompiler_temp;
        if (JSCompiler_temp = !atEof && from == to) {
          if (JSCompiler_temp = from, /\(\)|\[\]|\{\}/.test(state$1.sliceDoc(JSCompiler_temp - 1, JSCompiler_temp + 1))) {
            JSCompiler_temp = {from:JSCompiler_temp, to:JSCompiler_temp};
          } else {
            var context = language.syntaxTree(state$1).resolveInner(JSCompiler_temp), before = context.childBefore(JSCompiler_temp);
            context = context.childAfter(JSCompiler_temp);
            var closedBy;
            JSCompiler_temp = before && context && before.to <= JSCompiler_temp && context.from >= JSCompiler_temp && (closedBy = before.type.prop(common.NodeProp.closedBy)) && -1 < closedBy.indexOf(context.name) && state$1.doc.lineAt(before.to).from == state$1.doc.lineAt(context.from).from && !/\S/.test(state$1.sliceDoc(before.to, context.from)) ? {from:before.to, to:context.from} : null;
          }
        }
        closedBy = JSCompiler_temp;
        atEof && (from = to = (to <= range.to ? range : state$1.doc.lineAt(to)).to);
        before = new language.IndentContext(state$1, {simulateBreak:from, simulateDoubleBreak:!!closedBy});
        JSCompiler_temp = language.getIndentation(before, from);
        for (null == JSCompiler_temp && (JSCompiler_temp = state.countColumn(/^\s*/.exec(state$1.doc.lineAt(from).text)[0], state$1.tabSize)); to < range.to && /\s/.test(range.text[to - range.from]);) {
          to++;
        }
        closedBy ? {from, to} = closedBy : from > range.from && from < range.from + 100 && !/\S/.test(range.text.slice(0, from)) && (from = range.from);
        JSCompiler_temp = ["", language.indentString(state$1, JSCompiler_temp)];
        closedBy && JSCompiler_temp.push(language.indentString(state$1, before.lineIndent(range.from, -1)));
        return {changes:{from, to, insert:state.Text.of(JSCompiler_temp)}, range:state.EditorSelection.cursor(from + 1 + JSCompiler_temp[1].length)};
      });
      dispatch(state$1.update(changes, {scrollIntoView:!0, userEvent:"input"}));
      return !0;
    };
  }
  function changeBySelectedLine(state$1, f) {
    let atLine = -1;
    return state$1.changeByRange(range => {
      let changes = [];
      for (var pos = range.from; pos <= range.to;) {
        pos = state$1.doc.lineAt(pos), pos.number > atLine && (range.empty || range.to > pos.from) && (f(pos, changes, range), atLine = pos.number), pos = pos.to + 1;
      }
      pos = state$1.changes(changes);
      return {changes, range:state.EditorSelection.range(pos.mapPos(range.anchor, 1), pos.mapPos(range.head, 1))};
    });
  }
  var state = require("module$node_modules$$codemirror$state$dist$index_cjs"), view = require("module$node_modules$$codemirror$view$dist$index_cjs"), language = require("module$node_modules$$codemirror$language$dist$index_cjs"), common = require("module$node_modules$$lezer$common$dist$index_cjs");
  const toggleComment = target => {
    var {state} = target;
    state = state.doc.lineAt(state.selection.main.from);
    state = getConfig(target.state, state.from);
    return state.line ? toggleLineComment(target) : state.block ? toggleBlockCommentByLine(target) : !1;
  }, toggleLineComment = command(changeLineComment, 0), lineComment = command(changeLineComment, 1), lineUncomment = command(changeLineComment, 2), toggleBlockComment = command(changeBlockComment, 0), blockComment = command(changeBlockComment, 1), blockUncomment = command(changeBlockComment, 2), toggleBlockCommentByLine = command((o, s) => {
    let ranges = [];
    for (let r of s.selection.ranges) {
      let fromLine = s.doc.lineAt(r.from), toLine = r.to <= fromLine.to ? fromLine : s.doc.lineAt(r.to);
      toLine.from > fromLine.from && toLine.from == r.to && (toLine = r.to == fromLine.to + 1 ? fromLine : s.doc.lineAt(r.to - 1));
      let last = ranges.length - 1;
      0 <= last && ranges[last].to > fromLine.from ? ranges[last].to = toLine.to : ranges.push({from:fromLine.from + /^\s*/.exec(fromLine.text)[0].length, to:toLine.to});
    }
    return changeBlockComment(o, s, ranges);
  }, 0), fromHistory = state.Annotation.define(), isolateHistory = state.Annotation.define(), invertedEffects = state.Facet.define(), historyConfig = state.Facet.define({combine(configs) {
    return state.combineConfig(configs, {minDepth:100, newGroupDelay:500, joinToEvent:(_t, isAdjacent) => isAdjacent}, {minDepth:Math.max, newGroupDelay:Math.min, joinToEvent:(a, b) => (tr, adj) => a(tr, adj) || b(tr, adj)});
  }}), historyField_ = state.StateField.define({create() {
    return HistoryState.empty;
  }, update(state$1, tr) {
    let config = tr.state.facet(historyConfig);
    var fromHist = tr.annotation(fromHistory);
    if (fromHist) {
      var item = HistEvent.fromTransaction(tr, fromHist.selection), from = fromHist.side;
      state$1 = 0 == from ? state$1.undone : state$1.done;
      state$1 = item ? updateBranch(state$1, state$1.length, config.minDepth, item) : addSelection(state$1, tr.startState.selection);
      return new HistoryState(0 == from ? fromHist.rest : state$1, 0 == from ? state$1 : fromHist.rest);
    }
    fromHist = tr.annotation(isolateHistory);
    if ("full" == fromHist || "before" == fromHist) {
      state$1 = state$1.isolate();
    }
    if (!1 === tr.annotation(state.Transaction.addToHistory)) {
      return tr.changes.empty ? state$1 : state$1.addMapping(tr.changes.desc);
    }
    item = HistEvent.fromTransaction(tr);
    from = tr.annotation(state.Transaction.time);
    let userEvent = tr.annotation(state.Transaction.userEvent);
    item ? state$1 = state$1.addChanges(item, from, userEvent, config, tr) : tr.selection && (state$1 = state$1.addSelection(tr.startState.selection, from, userEvent, config.newGroupDelay));
    if ("full" == fromHist || "after" == fromHist) {
      state$1 = state$1.isolate();
    }
    return state$1;
  }, toJSON(value) {
    return {done:value.done.map(e => e.toJSON()), undone:value.undone.map(e => e.toJSON())};
  }, fromJSON(json) {
    return new HistoryState(json.done.map(HistEvent.fromJSON), json.undone.map(HistEvent.fromJSON));
  }}), historyField = historyField_, undo = cmd(0, !1), redo = cmd(1, !1), undoSelection = cmd(0, !0), redoSelection = cmd(1, !0), undoDepth = depth(0), redoDepth = depth(1);
  class HistEvent {
    constructor(changes, effects, mapped, startSelection, selectionsAfter) {
      this.changes = changes;
      this.effects = effects;
      this.mapped = mapped;
      this.startSelection = startSelection;
      this.selectionsAfter = selectionsAfter;
    }
    setSelAfter(after) {
      return new HistEvent(this.changes, this.effects, this.mapped, this.startSelection, after);
    }
    toJSON() {
      var _a, _b, _c;
      return {changes:null === (_a = this.changes) || void 0 === _a ? void 0 : _a.toJSON(), mapped:null === (_b = this.mapped) || void 0 === _b ? void 0 : _b.toJSON(), startSelection:null === (_c = this.startSelection) || void 0 === _c ? void 0 : _c.toJSON(), selectionsAfter:this.selectionsAfter.map(s => s.toJSON())};
    }
    static fromJSON(json) {
      return new HistEvent(json.changes && state.ChangeSet.fromJSON(json.changes), [], json.mapped && state.ChangeDesc.fromJSON(json.mapped), json.startSelection && state.EditorSelection.fromJSON(json.startSelection), json.selectionsAfter.map(state.EditorSelection.fromJSON));
    }
    static fromTransaction(tr, selection) {
      let effects = none;
      for (let invert of tr.startState.facet(invertedEffects)) {
        let result = invert(tr);
        result.length && (effects = effects.concat(result));
      }
      return !effects.length && tr.changes.empty ? null : new HistEvent(tr.changes.invert(tr.startState.doc), effects, void 0, selection || tr.startState.selection, none);
    }
    static selection(selections) {
      return new HistEvent(void 0, none, void 0, void 0, selections);
    }
  }
  const none = [], joinableUserEvent = /^(input\.type|delete)($|\.)/;
  class HistoryState {
    constructor(done, undone, prevTime = 0, prevUserEvent) {
      this.done = done;
      this.undone = undone;
      this.prevTime = prevTime;
      this.prevUserEvent = prevUserEvent;
    }
    isolate() {
      return this.prevTime ? new HistoryState(this.done, this.undone) : this;
    }
    addChanges(event, time, userEvent, config, tr) {
      let done = this.done, lastEvent = done[done.length - 1];
      done = lastEvent && lastEvent.changes && !lastEvent.changes.empty && event.changes && (!userEvent || joinableUserEvent.test(userEvent)) && (!lastEvent.selectionsAfter.length && time - this.prevTime < config.newGroupDelay && config.joinToEvent(tr, isAdjacent(lastEvent.changes, event.changes)) || "input.type.compose" == userEvent) ? updateBranch(done, done.length - 1, config.minDepth, new HistEvent(event.changes.compose(lastEvent.changes), conc(state.StateEffect.mapEffects(event.effects, lastEvent.changes), 
      lastEvent.effects), lastEvent.mapped, lastEvent.startSelection, none)) : updateBranch(done, done.length, config.minDepth, event);
      return new HistoryState(done, none, time, userEvent);
    }
    addSelection(selection, time, userEvent, newGroupDelay) {
      let last = this.done.length ? this.done[this.done.length - 1].selectionsAfter : none;
      return 0 < last.length && time - this.prevTime < newGroupDelay && userEvent == this.prevUserEvent && userEvent && /^select($|\.)/.test(userEvent) && eqSelectionShape(last[last.length - 1], selection) ? this : new HistoryState(addSelection(this.done, selection), this.undone, time, userEvent);
    }
    addMapping(mapping) {
      return new HistoryState(addMappingToBranch(this.done, mapping), addMappingToBranch(this.undone, mapping), this.prevTime, this.prevUserEvent);
    }
    pop(side, state, onlySelection) {
      var branch = 0 == side ? this.done : this.undone;
      if (0 == branch.length) {
        return null;
      }
      var event = branch[branch.length - 1];
      let selection = event.selectionsAfter[0] || (event.startSelection ? event.startSelection.map(event.changes.invertedDesc, 1) : state.selection);
      if (onlySelection && event.selectionsAfter.length) {
        onlySelection = state.update;
        event = event.selectionsAfter[event.selectionsAfter.length - 1];
        var JSCompiler_temp_const = fromHistory.of;
        let last = branch[branch.length - 1], newBranch = branch.slice();
        newBranch[branch.length - 1] = last.setSelAfter(last.selectionsAfter.slice(0, last.selectionsAfter.length - 1));
        return onlySelection.call(state, {selection:event, annotations:JSCompiler_temp_const.call(fromHistory, {side, rest:newBranch, selection}), userEvent:0 == side ? "select.undo" : "select.redo", scrollIntoView:!0});
      }
      return event.changes ? (branch = 1 == branch.length ? none : branch.slice(0, branch.length - 1), event.mapped && (branch = addMappingToBranch(branch, event.mapped)), state.update({changes:event.changes, selection:event.startSelection, effects:event.effects, annotations:fromHistory.of({side, rest:branch, selection}), filter:!1, userEvent:0 == side ? "undo" : "redo", scrollIntoView:!0})) : null;
    }
  }
  HistoryState.empty = new HistoryState(none, none);
  const historyKeymap = [{key:"Mod-z", run:undo, preventDefault:!0}, {key:"Mod-y", mac:"Mod-Shift-z", run:redo, preventDefault:!0}, {linux:"Ctrl-Shift-z", run:redo, preventDefault:!0}, {key:"Mod-u", run:undoSelection, preventDefault:!0}, {key:"Alt-u", mac:"Mod-Shift-u", run:redoSelection, preventDefault:!0}], cursorCharLeft = view => cursorByChar(view, !ltrAtCursor(view)), cursorCharRight = view => cursorByChar(view, ltrAtCursor(view)), cursorGroupLeft = view => cursorByGroup(view, !ltrAtCursor(view)), 
  cursorGroupRight = view => cursorByGroup(view, ltrAtCursor(view)), segmenter = "undefined" != typeof Intl && Intl.Segmenter ? new Intl.Segmenter(void 0, {granularity:"word"}) : null, cursorSyntaxLeft = view => moveSel(view, range => moveBySyntax(view.state, range, !ltrAtCursor(view))), cursorSyntaxRight = view => moveSel(view, range => moveBySyntax(view.state, range, ltrAtCursor(view))), cursorLineUp = view => cursorByLine(view, !1), cursorLineDown = view => cursorByLine(view, !0), cursorPageUp = 
  view => cursorByPage(view, !1), cursorPageDown = view => cursorByPage(view, !0), cursorLineBoundaryForward = view => moveSel(view, range => moveByLineBoundary(view, range, !0)), cursorLineBoundaryBackward = view => moveSel(view, range => moveByLineBoundary(view, range, !1)), cursorLineBoundaryLeft = view => moveSel(view, range => moveByLineBoundary(view, range, !ltrAtCursor(view))), cursorLineBoundaryRight = view => moveSel(view, range => moveByLineBoundary(view, range, ltrAtCursor(view))), cursorLineStart = 
  view => moveSel(view, range => state.EditorSelection.cursor(view.lineBlockAt(range.head).from, 1)), cursorLineEnd = view => moveSel(view, range => state.EditorSelection.cursor(view.lineBlockAt(range.head).to, -1)), cursorMatchingBracket = ({state, dispatch}) => toMatchingBracket(state, dispatch, !1), selectCharLeft = view => selectByChar(view, !ltrAtCursor(view)), selectCharRight = view => selectByChar(view, ltrAtCursor(view)), selectGroupLeft = view => selectByGroup(view, !ltrAtCursor(view)), 
  selectGroupRight = view => selectByGroup(view, ltrAtCursor(view)), selectSyntaxLeft = view => extendSel(view, range => moveBySyntax(view.state, range, !ltrAtCursor(view))), selectSyntaxRight = view => extendSel(view, range => moveBySyntax(view.state, range, ltrAtCursor(view))), selectLineUp = view => selectByLine(view, !1), selectLineDown = view => selectByLine(view, !0), selectPageUp = view => selectByPage(view, !1), selectPageDown = view => selectByPage(view, !0), selectLineBoundaryForward = 
  view => extendSel(view, range => moveByLineBoundary(view, range, !0)), selectLineBoundaryBackward = view => extendSel(view, range => moveByLineBoundary(view, range, !1)), selectLineBoundaryLeft = view => extendSel(view, range => moveByLineBoundary(view, range, !ltrAtCursor(view))), selectLineBoundaryRight = view => extendSel(view, range => moveByLineBoundary(view, range, ltrAtCursor(view))), selectLineStart = view => extendSel(view, range => state.EditorSelection.cursor(view.lineBlockAt(range.head).from)), 
  selectLineEnd = view => extendSel(view, range => state.EditorSelection.cursor(view.lineBlockAt(range.head).to)), cursorDocStart = ({state, dispatch}) => {
    dispatch(setSel(state, {anchor:0}));
    return !0;
  }, cursorDocEnd = ({state, dispatch}) => {
    dispatch(setSel(state, {anchor:state.doc.length}));
    return !0;
  }, selectDocStart = ({state, dispatch}) => {
    dispatch(setSel(state, {anchor:state.selection.main.anchor, head:0}));
    return !0;
  }, selectDocEnd = ({state, dispatch}) => {
    dispatch(setSel(state, {anchor:state.selection.main.anchor, head:state.doc.length}));
    return !0;
  }, selectAll = ({state, dispatch}) => {
    dispatch(state.update({selection:{anchor:0, head:state.doc.length}, userEvent:"select"}));
    return !0;
  }, selectLine = ({state:state$1, dispatch}) => {
    let ranges = selectedLineBlocks(state$1).map(({from, to}) => state.EditorSelection.range(from, Math.min(to + 1, state$1.doc.length)));
    dispatch(state$1.update({selection:state.EditorSelection.create(ranges), userEvent:"select"}));
    return !0;
  }, selectParentSyntax = ({state:state$1, dispatch}) => {
    let selection = updateSel(state$1.selection, range => {
      var tree = language.syntaxTree(state$1), stack = tree.resolveStack(range.from, 1);
      range.empty && (tree = tree.resolveStack(range.from, -1), tree.node.from >= stack.node.from && tree.node.to <= stack.node.to && (stack = tree));
      for (; stack; stack = stack.next) {
        if ({node:tree} = stack, (tree.from < range.from && tree.to >= range.to || tree.to > range.to && tree.from <= range.from) && stack.next) {
          return state.EditorSelection.range(tree.to, tree.from);
        }
      }
      return range;
    });
    if (selection.eq(state$1.selection)) {
      return !1;
    }
    dispatch(setSel(state$1, selection));
    return !0;
  }, addCursorAbove = view => addCursorVertically(view, !1), addCursorBelow = view => addCursorVertically(view, !0), simplifySelection = ({state:state$1, dispatch}) => {
    let cur = state$1.selection, selection = null;
    1 < cur.ranges.length ? selection = state.EditorSelection.create([cur.main]) : cur.main.empty || (selection = state.EditorSelection.create([state.EditorSelection.cursor(cur.main.head)]));
    if (!selection) {
      return !1;
    }
    dispatch(setSel(state$1, selection));
    return !0;
  }, deleteByChar = (target, forward, byIndentUnit) => deleteBy(target, range => {
    range = range.from;
    var {state:state$1} = target, line = state$1.doc.lineAt(range), before;
    if (byIndentUnit && !forward && range > line.from && range < line.from + 200 && !/[^ \t]/.test(before = line.text.slice(0, range - line.from))) {
      if ("\t" == before[before.length - 1]) {
        return range - 1;
      }
      state$1 = state.countColumn(before, state$1.tabSize) % language.getIndentUnit(state$1) || language.getIndentUnit(state$1);
      for (line = 0; line < state$1 && " " == before[before.length - 1 - line]; line++) {
        range--;
      }
      before = range;
    } else {
      before = state.findClusterBreak(line.text, range - line.from, forward, forward) + line.from, before == range && line.number != (forward ? state$1.doc.lines : 1) ? before += forward ? 1 : -1 : !forward && /[\ufe00-\ufe0f]/.test(line.text.slice(before - line.from, range - line.from)) && (before = state.findClusterBreak(line.text, before - line.from, !1, !1) + line.from);
    }
    return before;
  }), deleteCharBackward = view => deleteByChar(view, !1, !0), deleteCharForward = view => deleteByChar(view, !0, !1), deleteByGroup = (target, forward) => deleteBy(target, range => {
    let pos = range.head, {state:state$1} = target, line = state$1.doc.lineAt(pos), categorize = state$1.charCategorizer(pos);
    for (let cat = null;;) {
      if (pos == (forward ? line.to : line.from)) {
        pos == range.head && line.number != (forward ? state$1.doc.lines : 1) && (pos += forward ? 1 : -1);
        break;
      }
      let next = state.findClusterBreak(line.text, pos - line.from, forward) + line.from, nextChar = line.text.slice(Math.min(pos, next) - line.from, Math.max(pos, next) - line.from), nextCat = categorize(nextChar);
      if (null != cat && nextCat != cat) {
        break;
      }
      if (" " != nextChar || pos != range.head) {
        cat = nextCat;
      }
      pos = next;
    }
    return pos;
  }), deleteGroupBackward = target => deleteByGroup(target, !1), deleteGroupForward = target => deleteByGroup(target, !0), deleteToLineEnd = view => deleteBy(view, range => {
    let lineEnd = view.lineBlockAt(range.head).to;
    return range.head < lineEnd ? lineEnd : Math.min(view.state.doc.length, range.head + 1);
  }), deleteLineBoundaryBackward = view => deleteBy(view, range => {
    let lineStart = view.moveToLineBoundary(range, !1).head;
    return range.head > lineStart ? lineStart : Math.max(0, range.head - 1);
  }), deleteLineBoundaryForward = view => deleteBy(view, range => {
    let lineStart = view.moveToLineBoundary(range, !0).head;
    return range.head < lineStart ? lineStart : Math.min(view.state.doc.length, range.head + 1);
  }), splitLine = ({state:state$1, dispatch}) => {
    if (state$1.readOnly) {
      return !1;
    }
    let changes = state$1.changeByRange(range => ({changes:{from:range.from, to:range.to, insert:state.Text.of(["", ""])}, range:state.EditorSelection.cursor(range.from)}));
    dispatch(state$1.update(changes, {scrollIntoView:!0, userEvent:"input"}));
    return !0;
  }, transposeChars = ({state:state$1, dispatch}) => {
    if (state$1.readOnly) {
      return !1;
    }
    let changes = state$1.changeByRange(range => {
      if (!range.empty || 0 == range.from || range.from == state$1.doc.length) {
        return {range};
      }
      range = range.from;
      var line = state$1.doc.lineAt(range);
      let from = range == line.from ? range - 1 : state.findClusterBreak(line.text, range - line.from, !1) + line.from;
      line = range == line.to ? range + 1 : state.findClusterBreak(line.text, range - line.from, !0) + line.from;
      return {changes:{from, to:line, insert:state$1.doc.slice(range, line).append(state$1.doc.slice(from, range))}, range:state.EditorSelection.cursor(line)};
    });
    if (changes.changes.empty) {
      return !1;
    }
    dispatch(state$1.update(changes, {scrollIntoView:!0, userEvent:"move.character"}));
    return !0;
  }, moveLineUp = ({state, dispatch}) => moveLine(state, dispatch, !1), moveLineDown = ({state, dispatch}) => moveLine(state, dispatch, !0), copyLineUp = ({state, dispatch}) => copyLine(state, dispatch, !1), copyLineDown = ({state, dispatch}) => copyLine(state, dispatch, !0), deleteLine = view => {
    if (view.state.readOnly) {
      return !1;
    }
    let {state} = view, changes = state.changes(selectedLineBlocks(state).map(({from, to}) => {
      0 < from ? from-- : to < state.doc.length && to++;
      return {from, to};
    })), selection = updateSel(state.selection, range => {
      let dist = void 0;
      if (view.lineWrapping) {
        let block = view.lineBlockAt(range.head), pos = view.coordsAtPos(range.head, range.assoc || 1);
        pos && (dist = block.bottom + view.documentTop - pos.bottom + view.defaultLineHeight / 2);
      }
      return view.moveVertically(range, !0, dist);
    }).map(changes);
    view.dispatch({changes, selection, scrollIntoView:!0, userEvent:"delete.line"});
    return !0;
  }, insertNewlineAndIndent = newlineAndIndent(!1), insertBlankLine = newlineAndIndent(!0), indentSelection = ({state, dispatch}) => {
    if (state.readOnly) {
      return !1;
    }
    let updated = Object.create(null), context = new language.IndentContext(state, {overrideIndentation:start => {
      start = updated[start];
      return null == start ? -1 : start;
    }}), changes = changeBySelectedLine(state, (line, changes, range) => {
      let indent = language.getIndentation(context, line.from);
      if (null != indent) {
        /\S/.test(line.text) || (indent = 0);
        var cur = /^\s*/.exec(line.text)[0], norm = language.indentString(state, indent);
        if (cur != norm || range.from < line.from + cur.length) {
          updated[line.from] = indent, changes.push({from:line.from, to:line.from + cur.length, insert:norm});
        }
      }
    });
    changes.changes.empty || dispatch(state.update(changes, {userEvent:"indent"}));
    return !0;
  }, indentMore = ({state, dispatch}) => {
    if (state.readOnly) {
      return !1;
    }
    dispatch(state.update(changeBySelectedLine(state, (line, changes) => {
      changes.push({from:line.from, insert:state.facet(language.indentUnit)});
    }), {userEvent:"input.indent"}));
    return !0;
  }, indentLess = ({state:state$1, dispatch}) => {
    if (state$1.readOnly) {
      return !1;
    }
    dispatch(state$1.update(changeBySelectedLine(state$1, (line, changes) => {
      let space = /^\s*/.exec(line.text)[0];
      if (space) {
        var col = state.countColumn(space, state$1.tabSize), keep = 0;
        for (col = language.indentString(state$1, Math.max(0, col - language.getIndentUnit(state$1))); keep < space.length && keep < col.length && space.charCodeAt(keep) == col.charCodeAt(keep);) {
          keep++;
        }
        changes.push({from:line.from + keep, to:line.from + space.length, insert:col.slice(keep)});
      }
    }), {userEvent:"delete.dedent"}));
    return !0;
  }, toggleTabFocusMode = view => {
    view.setTabFocusMode();
    return !0;
  }, emacsStyleKeymap = [{key:"Ctrl-b", run:cursorCharLeft, shift:selectCharLeft, preventDefault:!0}, {key:"Ctrl-f", run:cursorCharRight, shift:selectCharRight}, {key:"Ctrl-p", run:cursorLineUp, shift:selectLineUp}, {key:"Ctrl-n", run:cursorLineDown, shift:selectLineDown}, {key:"Ctrl-a", run:cursorLineStart, shift:selectLineStart}, {key:"Ctrl-e", run:cursorLineEnd, shift:selectLineEnd}, {key:"Ctrl-d", run:deleteCharForward}, {key:"Ctrl-h", run:deleteCharBackward}, {key:"Ctrl-k", run:deleteToLineEnd}, 
  {key:"Ctrl-Alt-h", run:deleteGroupBackward}, {key:"Ctrl-o", run:splitLine}, {key:"Ctrl-t", run:transposeChars}, {key:"Ctrl-v", run:cursorPageDown}], standardKeymap = [{key:"ArrowLeft", run:cursorCharLeft, shift:selectCharLeft, preventDefault:!0}, {key:"Mod-ArrowLeft", mac:"Alt-ArrowLeft", run:cursorGroupLeft, shift:selectGroupLeft, preventDefault:!0}, {mac:"Cmd-ArrowLeft", run:cursorLineBoundaryLeft, shift:selectLineBoundaryLeft, preventDefault:!0}, {key:"ArrowRight", run:cursorCharRight, shift:selectCharRight, 
  preventDefault:!0}, {key:"Mod-ArrowRight", mac:"Alt-ArrowRight", run:cursorGroupRight, shift:selectGroupRight, preventDefault:!0}, {mac:"Cmd-ArrowRight", run:cursorLineBoundaryRight, shift:selectLineBoundaryRight, preventDefault:!0}, {key:"ArrowUp", run:cursorLineUp, shift:selectLineUp, preventDefault:!0}, {mac:"Cmd-ArrowUp", run:cursorDocStart, shift:selectDocStart}, {mac:"Ctrl-ArrowUp", run:cursorPageUp, shift:selectPageUp}, {key:"ArrowDown", run:cursorLineDown, shift:selectLineDown, preventDefault:!0}, 
  {mac:"Cmd-ArrowDown", run:cursorDocEnd, shift:selectDocEnd}, {mac:"Ctrl-ArrowDown", run:cursorPageDown, shift:selectPageDown}, {key:"PageUp", run:cursorPageUp, shift:selectPageUp}, {key:"PageDown", run:cursorPageDown, shift:selectPageDown}, {key:"Home", run:cursorLineBoundaryBackward, shift:selectLineBoundaryBackward, preventDefault:!0}, {key:"Mod-Home", run:cursorDocStart, shift:selectDocStart}, {key:"End", run:cursorLineBoundaryForward, shift:selectLineBoundaryForward, preventDefault:!0}, {key:"Mod-End", 
  run:cursorDocEnd, shift:selectDocEnd}, {key:"Enter", run:insertNewlineAndIndent, shift:insertNewlineAndIndent}, {key:"Mod-a", run:selectAll}, {key:"Backspace", run:deleteCharBackward, shift:deleteCharBackward, preventDefault:!0}, {key:"Delete", run:deleteCharForward, preventDefault:!0}, {key:"Mod-Backspace", mac:"Alt-Backspace", run:deleteGroupBackward, preventDefault:!0}, {key:"Mod-Delete", mac:"Alt-Delete", run:deleteGroupForward, preventDefault:!0}, {mac:"Mod-Backspace", run:deleteLineBoundaryBackward, 
  preventDefault:!0}, {mac:"Mod-Delete", run:deleteLineBoundaryForward, preventDefault:!0}].concat(emacsStyleKeymap.map(b => ({mac:b.key, run:b.run, shift:b.shift}))), defaultKeymap = [{key:"Alt-ArrowLeft", mac:"Ctrl-ArrowLeft", run:cursorSyntaxLeft, shift:selectSyntaxLeft}, {key:"Alt-ArrowRight", mac:"Ctrl-ArrowRight", run:cursorSyntaxRight, shift:selectSyntaxRight}, {key:"Alt-ArrowUp", run:moveLineUp}, {key:"Shift-Alt-ArrowUp", run:copyLineUp}, {key:"Alt-ArrowDown", run:moveLineDown}, {key:"Shift-Alt-ArrowDown", 
  run:copyLineDown}, {key:"Mod-Alt-ArrowUp", run:addCursorAbove}, {key:"Mod-Alt-ArrowDown", run:addCursorBelow}, {key:"Escape", run:simplifySelection}, {key:"Mod-Enter", run:insertBlankLine}, {key:"Alt-l", mac:"Ctrl-l", run:selectLine}, {key:"Mod-i", run:selectParentSyntax, preventDefault:!0}, {key:"Mod-[", run:indentLess}, {key:"Mod-]", run:indentMore}, {key:"Mod-Alt-\\", run:indentSelection}, {key:"Shift-Mod-k", run:deleteLine}, {key:"Shift-Mod-\\", run:cursorMatchingBracket}, {key:"Mod-/", run:toggleComment}, 
  {key:"Alt-A", run:toggleBlockComment}, {key:"Ctrl-m", mac:"Shift-Alt-m", run:toggleTabFocusMode}].concat(standardKeymap), indentWithTab = {key:"Tab", run:indentMore, shift:indentLess};
  exports.addCursorAbove = addCursorAbove;
  exports.addCursorBelow = addCursorBelow;
  exports.blockComment = blockComment;
  exports.blockUncomment = blockUncomment;
  exports.copyLineDown = copyLineDown;
  exports.copyLineUp = copyLineUp;
  exports.cursorCharBackward = view => cursorByChar(view, !1);
  exports.cursorCharBackwardLogical = target => moveByCharLogical(target, !1);
  exports.cursorCharForward = view => cursorByChar(view, !0);
  exports.cursorCharForwardLogical = target => moveByCharLogical(target, !0);
  exports.cursorCharLeft = cursorCharLeft;
  exports.cursorCharRight = cursorCharRight;
  exports.cursorDocEnd = cursorDocEnd;
  exports.cursorDocStart = cursorDocStart;
  exports.cursorGroupBackward = view => cursorByGroup(view, !1);
  exports.cursorGroupForward = view => cursorByGroup(view, !0);
  exports.cursorGroupForwardWin = view => moveSel(view, range => range.empty ? view.moveByChar(range, !0, start => toGroupStart(view, range.head, start)) : rangeEnd(range, !0));
  exports.cursorGroupLeft = cursorGroupLeft;
  exports.cursorGroupRight = cursorGroupRight;
  exports.cursorLineBoundaryBackward = cursorLineBoundaryBackward;
  exports.cursorLineBoundaryForward = cursorLineBoundaryForward;
  exports.cursorLineBoundaryLeft = cursorLineBoundaryLeft;
  exports.cursorLineBoundaryRight = cursorLineBoundaryRight;
  exports.cursorLineDown = cursorLineDown;
  exports.cursorLineEnd = cursorLineEnd;
  exports.cursorLineStart = cursorLineStart;
  exports.cursorLineUp = cursorLineUp;
  exports.cursorMatchingBracket = cursorMatchingBracket;
  exports.cursorPageDown = cursorPageDown;
  exports.cursorPageUp = cursorPageUp;
  exports.cursorSubwordBackward = view => cursorBySubword(view, !1);
  exports.cursorSubwordForward = view => cursorBySubword(view, !0);
  exports.cursorSyntaxLeft = cursorSyntaxLeft;
  exports.cursorSyntaxRight = cursorSyntaxRight;
  exports.defaultKeymap = defaultKeymap;
  exports.deleteCharBackward = deleteCharBackward;
  exports.deleteCharBackwardStrict = view => deleteByChar(view, !1, !1);
  exports.deleteCharForward = deleteCharForward;
  exports.deleteGroupBackward = deleteGroupBackward;
  exports.deleteGroupForward = deleteGroupForward;
  exports.deleteGroupForwardWin = view => deleteBy(view, range => view.moveByChar(range, !0, start => toGroupStart(view, range.head, start)).head);
  exports.deleteLine = deleteLine;
  exports.deleteLineBoundaryBackward = deleteLineBoundaryBackward;
  exports.deleteLineBoundaryForward = deleteLineBoundaryForward;
  exports.deleteToLineEnd = deleteToLineEnd;
  exports.deleteToLineStart = view => deleteBy(view, range => {
    let lineStart = view.lineBlockAt(range.head).from;
    return range.head > lineStart ? lineStart : Math.max(0, range.head - 1);
  });
  exports.deleteTrailingWhitespace = ({state, dispatch}) => {
    if (state.readOnly) {
      return !1;
    }
    let changes = [];
    for (let pos = 0, prev = "", iter = state.doc.iter();;) {
      iter.next();
      if (iter.lineBreak || iter.done) {
        let trailing = prev.search(/\s+$/);
        -1 < trailing && changes.push({from:pos - (prev.length - trailing), to:pos});
        if (iter.done) {
          break;
        }
        prev = "";
      } else {
        prev = iter.value;
      }
      pos += iter.value.length;
    }
    if (!changes.length) {
      return !1;
    }
    dispatch(state.update({changes, userEvent:"delete"}));
    return !0;
  };
  exports.emacsStyleKeymap = emacsStyleKeymap;
  exports.history = function(config = {}) {
    return [historyField_, historyConfig.of(config), view.EditorView.domEventHandlers({beforeinput(e, view) {
      let command = "historyUndo" == e.inputType ? undo : "historyRedo" == e.inputType ? redo : null;
      if (!command) {
        return !1;
      }
      e.preventDefault();
      return command(view);
    }})];
  };
  exports.historyField = historyField;
  exports.historyKeymap = historyKeymap;
  exports.indentLess = indentLess;
  exports.indentMore = indentMore;
  exports.indentSelection = indentSelection;
  exports.indentWithTab = indentWithTab;
  exports.insertBlankLine = insertBlankLine;
  exports.insertNewline = ({state, dispatch}) => {
    dispatch(state.update(state.replaceSelection(state.lineBreak), {scrollIntoView:!0, userEvent:"input"}));
    return !0;
  };
  exports.insertNewlineAndIndent = insertNewlineAndIndent;
  exports.insertNewlineKeepIndent = ({state:state$1, dispatch}) => {
    dispatch(state$1.update(state$1.changeByRange(range => {
      let indent = /^\s*/.exec(state$1.doc.lineAt(range.from).text)[0];
      return {changes:{from:range.from, to:range.to, insert:state$1.lineBreak + indent}, range:state.EditorSelection.cursor(range.from + indent.length + 1)};
    }), {scrollIntoView:!0, userEvent:"input"}));
    return !0;
  };
  exports.insertTab = ({state, dispatch}) => {
    if (state.selection.ranges.some(r => !r.empty)) {
      return indentMore({state, dispatch});
    }
    dispatch(state.update(state.replaceSelection("\t"), {scrollIntoView:!0, userEvent:"input"}));
    return !0;
  };
  exports.invertedEffects = invertedEffects;
  exports.isolateHistory = isolateHistory;
  exports.lineComment = lineComment;
  exports.lineUncomment = lineUncomment;
  exports.moveLineDown = moveLineDown;
  exports.moveLineUp = moveLineUp;
  exports.redo = redo;
  exports.redoDepth = redoDepth;
  exports.redoSelection = redoSelection;
  exports.selectAll = selectAll;
  exports.selectCharBackward = view => selectByChar(view, !1);
  exports.selectCharBackwardLogical = target => extendSel(target, range => byCharLogical(target.state, range, !1));
  exports.selectCharForward = view => selectByChar(view, !0);
  exports.selectCharForwardLogical = target => extendSel(target, range => byCharLogical(target.state, range, !0));
  exports.selectCharLeft = selectCharLeft;
  exports.selectCharRight = selectCharRight;
  exports.selectDocEnd = selectDocEnd;
  exports.selectDocStart = selectDocStart;
  exports.selectGroupBackward = view => selectByGroup(view, !1);
  exports.selectGroupForward = view => selectByGroup(view, !0);
  exports.selectGroupForwardWin = view => extendSel(view, range => view.moveByChar(range, !0, start => toGroupStart(view, range.head, start)));
  exports.selectGroupLeft = selectGroupLeft;
  exports.selectGroupRight = selectGroupRight;
  exports.selectLine = selectLine;
  exports.selectLineBoundaryBackward = selectLineBoundaryBackward;
  exports.selectLineBoundaryForward = selectLineBoundaryForward;
  exports.selectLineBoundaryLeft = selectLineBoundaryLeft;
  exports.selectLineBoundaryRight = selectLineBoundaryRight;
  exports.selectLineDown = selectLineDown;
  exports.selectLineEnd = selectLineEnd;
  exports.selectLineStart = selectLineStart;
  exports.selectLineUp = selectLineUp;
  exports.selectMatchingBracket = ({state, dispatch}) => toMatchingBracket(state, dispatch, !0);
  exports.selectPageDown = selectPageDown;
  exports.selectPageUp = selectPageUp;
  exports.selectParentSyntax = selectParentSyntax;
  exports.selectSubwordBackward = view => selectBySubword(view, !1);
  exports.selectSubwordForward = view => selectBySubword(view, !0);
  exports.selectSyntaxLeft = selectSyntaxLeft;
  exports.selectSyntaxRight = selectSyntaxRight;
  exports.simplifySelection = simplifySelection;
  exports.splitLine = splitLine;
  exports.standardKeymap = standardKeymap;
  exports.temporarilySetTabFocusMode = view => {
    view.setTabFocusMode(2000);
    return !0;
  };
  exports.toggleBlockComment = toggleBlockComment;
  exports.toggleBlockCommentByLine = toggleBlockCommentByLine;
  exports.toggleComment = toggleComment;
  exports.toggleLineComment = toggleLineComment;
  exports.toggleTabFocusMode = toggleTabFocusMode;
  exports.transposeChars = transposeChars;
  exports.undo = undo;
  exports.undoDepth = undoDepth;
  exports.undoSelection = undoSelection;
};

//# sourceMappingURL=module$node_modules$$codemirror$commands$dist$index_cjs.js.map
