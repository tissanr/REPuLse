{
  "name": "repulse",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/lisp",
    "app"
  ],
  "scripts": {
    "dev": "npx shadow-cljs watch app",
    "test": "npx shadow-cljs compile test && node out/test.js"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.0"
  }
}
